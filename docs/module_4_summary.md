In this module, you built the entire frontend interface of your blog application using React. You created reusable components like Post, PostList, CreatePost, PostFilter, and PostSorting, assembling them into a complete UI layout. At first, the components were static (focused only on structure) but you gradually added dynamic behavior using state, props, and hooks.

Next, you connected your React app to your backend using TanStack Query. You configured a QueryClient, used useQuery to fetch posts, and rendered real data from your backend into your frontend. You even added the ability to filter and sort posts in real time by managing state and query parameters; creating a highly responsive experience for users.

Finally, you implemented post creation using forms and useMutation. With TanStack Query’s cache invalidation, your app updated instantly when new posts were added; no manual refresh needed. At this point, your application officially crossed into full-stack territory: frontend and backend are fully integrated, and the user interface is dynamic, data-driven, and functional. You now have a real full-stack blog application in your hands, and you're ready to take it even further.
